<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.araba.cuma.araba">

    <uses-permission android:nameSurname="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:nameSurname=".Activity.RegisterActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:nameSurname="android.intent.action.MAIN" />

                <category android:nameSurname="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:nameSurname=".Activity.MainActivity" />
        <activity android:nameSurname=".Activity.WelcomeActivity" />
        <activity android:nameSurname=".Activity.LoginActivity" />
        <activity android:nameSurname=".Activity.MesajActivity" />
    </application>

</manifest>